# All scrapers use only the Python standard library (urllib, ssl, json, re, csv).
# No third-party packages are required to run the scrapers.

# Optional — needed only for utils/make_progress_charts.py
openpyxl>=3.1
pandas>=2.0
matplotlib>=3.7
